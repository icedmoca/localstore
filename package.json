{
  "name": "localstore",
  "version": "1.0.0",
  "description": "Local tool store with development capabilities",
  "main": "index.js",
  "scripts": {
    "backend": "cd backend && python3 app.py",
    "frontend": "cd frontend && vite --host",
    "dev": "concurrently -k -n back,front -c blue,magenta \"npm:backend\" \"npm:frontend\"",
    "build": "cd frontend && npm run build",
    "electron": "cd electron && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../electron && npm install",
    "setup": "npm run install-all && npm run build",
    "test:contract": "bash backend/contract_test.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
